Menu 1 (nav, -)
  ABC (nav, -)
    A (btn)
    B (btn)
    C (btn)
  DE (nav, -)
    D (btn)
    E (btn)
Menu 2 (nav, Example Title)
  Sub Menu (nav, You can have submenus)
    Sub Sub Menu (nav, Nested in menus)
      Sub sub sub Menu (nav, Which are even more nested)
        Hello (btn)
  ExecButton (btn, writeToScreen)